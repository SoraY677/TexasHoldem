import java.awt.*
import javax.swing.*

class fieldView : JFrame("hogehoge"){
    init {
        setSize(500,500)
        layout = BorderLayout()
        defaultCloseOperation = JFrame.EXIT_ON_CLOSE
        setVisible(true)
    }

}